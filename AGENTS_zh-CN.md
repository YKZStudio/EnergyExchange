# 项目约定

[English](AGENTS.md)

- 目标为 Minecraft 26.2 Fabric，验证后再扩大兼容范围。
- 文档默认英文，简体中文使用 `_zh-CN.md` 配套文件；两者同步维护。游戏内文字和模组名使用翻译键，不拼接双语名称。所有语言键完整且占位符一致。
- 每个正式版本发布 GitHub Release，包含运行 JAR 和双语日志，禁止静默替换已发布资产。
- 预发布使用 `0.2.0-pre2` 等版本号；通过测试并进入 main 后自动发行，正确标记预发布。
- 定价、知识、库存、经验与权限由服务端决定，客户端设置不能改变多人经济规则。
- 交易先预检后变更，不支持的组件变体必须拒绝，不能丢失或复制内容。
- 保留已有存档或显式迁移；重生测试使用完整服务端流程。
